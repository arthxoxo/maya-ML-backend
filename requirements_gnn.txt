# GNN Pipeline Dependencies
# Install: pip install -r requirements_gnn.txt

# PyTorch (CPU)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0

# PyTorch Geometric
torch-geometric>=2.5

# Existing deps (already installed)
pandas>=2.0
numpy>=1.24
textblob>=0.18
scikit-learn>=1.3
xgboost>=2.0
shap>=0.45
streamlit>=1.30
plotly>=5.18
confluent-kafka>=2.3
apache-flink>=2.0
networkx>=3.0
